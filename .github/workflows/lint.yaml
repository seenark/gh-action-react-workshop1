name: ESLint
on:
  push:
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v4

      - name: Install
        uses: ./.github/actions/install-and-caching

      - name: Run ESLint
        run: bun run lint
